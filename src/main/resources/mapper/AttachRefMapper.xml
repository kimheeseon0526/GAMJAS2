<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
   <mapper namespace="mapper.AttachRefMapper">
   
   <!-- 목록 조회  --> 
     <select id="list" resultType="AttachRef">
     	select * from tbl_attach_ref 
		where attach_ref_type = #{refType}
		and ref_no = #{refNo}
     </select>
     
   <!-- 추가  -->
    <insert id="insert"> 
	   	insert into tbl_attach_ref (attach_ref_type, ref_no)
		values (#{refType}, #{refNo})
    </insert>
    
    
   <!-- 삭제 -->
    <delete id="delete"> 
    	delete from tbl_attach_ref
		where attach_ref_type = #{refType}
		and ref_no = #{refNo}
    </delete>
    
 	</mapper>
   